This is my .emacs.d folder

* Prerequisites
All OS: git
Mac: ack
Debian: ack-grep

* Clone this repository
cd ~
git clone git://github.com/habib/.emacs.d.git
cd .emacs.d

* Get all the submodules
git submodule init
git submodule update

* Launch Emacs!